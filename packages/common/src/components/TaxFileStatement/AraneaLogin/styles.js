import { css } from '@emotion/core'
import styled from '@emotion/styled'
import { DianWidget as DianWidgetBase } from '@tributi-co/tributi-components'

export const DianWidget = styled(DianWidgetBase)(css`
  margin: 3rem 0 2rem;
`)
