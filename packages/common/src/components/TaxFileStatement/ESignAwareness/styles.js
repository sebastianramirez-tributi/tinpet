import { css } from '@emotion/core'
import styled from '@emotion/styled'

import { Button as ButtonBase } from '@tributi-co/tributi-components'

export const Button = styled(ButtonBase)(css`
  margin-top: 2rem;
`)

export const UserText = styled.span(css`
  margin-top: 1rem;
`)
